import React from "react";
function Card({ title, author, date, catgory, tags, content, id, img }) {
    return (
        <div>
            <h2>{title}</h2>
            <div></div>
        </div>
    );
}

export default Card;